app:
  appId: ${APP_ID}
  secret: ${APP_SECRET}
server:
  port: 80
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_ADDRESS}/${MYSQL_DATABASE:bzyWechat}
    username: ${MYSQL_USERNAME}
    password: ${MySQL_PASSWORD}
